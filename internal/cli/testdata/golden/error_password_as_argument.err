✗ smtp_password cannot be set as an argument, because an argument is visible in your
  shell history and in the process list.
  Set it with `mailkube auth login --smtp`, or through MAILKUBE_SMTP_PASSWORD.
